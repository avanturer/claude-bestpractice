<div align="center">

# claude-bestpractice

**Память, координация и принуждение для разработки продукта в нескольких параллельных сессиях Claude Code.**

[![version](https://img.shields.io/badge/version-1.5.0-black)](https://github.com/avanturer/claude-bestpractice/releases)
[![tests](https://img.shields.io/badge/tests-738%20passing-2ea44f)](#проверено)
[![doctor](https://img.shields.io/badge/doctor-26%20checks-2ea44f)](#проверено)
[![python](https://img.shields.io/badge/python-3.9%2B-blue)](#требования)
[![dependencies](https://img.shields.io/badge/dependencies-none-blue)](#требования)
[![context](https://img.shields.io/badge/постоянный%20контекст-332%20токена-blue)](#требования)
[![license](https://img.shields.io/badge/license-MIT-lightgrey)](../LICENSE)

[English](../README.md) · **Русский** · [中文](README.zh.md)

</div>

---

Уже внутри сессии Claude Code — самый короткий путь, терминал не нужен:

```
/plugin marketplace add avanturer/claude-bestpractice
/plugin install claude-bestpractice
```

Из терминала — то же самое:

```sh
claude plugin marketplace add avanturer/claude-bestpractice
claude plugin install claude-bestpractice@claude-bestpractice
```

Если первая строка полезет в `git@github.com:` и упрётся в отсутствующий SSH-ключ —
передайте URL вместо короткой формы. Это не гипотеза: на реальной установке короткая
форма разрешилась в SSH на машине без ключа.

```sh
claude plugin marketplace add https://github.com/avanturer/claude-bestpractice
```

Чего это не починит: глобальный `url.git@github.com:.insteadOf https://github.com/` в
вашем собственном git-конфиге перепишет и этот URL. Проверить —
`git config --get-regexp '^url\.'`.

Либо так — чтобы гейты были проверены на вашей машине **до** того, как что-либо
зарегистрируется:

```sh
curl -fsSL https://raw.githubusercontent.com/avanturer/claude-bestpractice/HEAD/install.sh | bash
```

Это не одно и то же, и разница видна в первый же день:

|  | `claude plugin install` | `install.sh` |
|---|---|---|
| Гейты работают в сессии | да | да |
| `claude-bp` **в вашем терминале** | нет | да (симлинки в `~/.local/bin`) |
| Доктор прогоняется до регистрации | нет | да |

Claude Code сам добавляет `bin/` плагина в PATH инструмента Bash, поэтому на пути через
маркетплейс команды ниже работают **внутри сессии** и дают `command not found` в вашей
оболочке.

Push-гейт при этом не ждёт ни одной из них. Первая сессия, запущенная в репозитории без
`pre-push` хука, ставит его и говорит об этом на доске; дальше молчит. `claude-bp-ci off`
его убирает, и отказ держится — следующая сессия не поставит хук обратно. Он ставится сам,
а не по команде, потому что гейт, срабатывающий только когда кто-то вспомнил его включить —
это ровно тот гейт, который весь этот проект и заменяет. Проверка нашла именно такой случай:
установка сообщала `✓ enabled` над путём push, на котором не стояло ничего.

Дальше в любом репозитории — из терминала или попросив Claude их выполнить:

```sh
claude-bp init      # вывести из вашего кода то, что выводится; спросить про остальное
claude-bp status    # всё сразу
```

Инсталлятор запускает доктора **до** регистрации плагина и отказывается ставиться, если
хотя бы один гейт не срабатывает. Гейт, который молча ничего не делает, хуже, чем его
отсутствие.

---

## Проблема

Вы строите продукт почти полностью через Claude. Одновременно работают три–восемь
сессий в отдельных worktree. Диффы вы почти не читаете. Что именно ломается — измерено,
а не выдумано:

| | |
|---|---|
| Агент говорит «готово», а код не работает | **0.97** сдач против **0.65** подтверждённых тестами. Два разных охранных промпта сдвинули это на **ноль** |
| Правит корректный код, который его не просили трогать | **60–90 %** прогонов на четырёх frontier-моделях в ситуациях, где правильно было воздержаться |
| Правила размываются по мере роста сессии | 0 % нарушений → **30 %** после одной компактификации → **78 %** после четырёх |
| Слишком много правил обрушивают соблюдение | **93.8 %** идеально при 10 правилах → **75 %** при 20 → **23.8 %** при 40 → **0 %** при 80 |
| Устаревший контекст хуже, чем никакого | Только устаревший retrieval дал вызовы мёртвых API в **15/17** примерах; без retrieval — **0/17** |

Источники по каждой цифре — в [`EVIDENCE.md`](EVIDENCE.md).

## Дизайн одним предложением

**Ничто важное не просится у модели.** Каждое правило, которое обязано соблюдаться,
принуждается харнессом или git; в контекст модели попадает только то немногое, что не
проверяется программой.

---

## Что вы получаете

### Память, которая не может протухнуть

Три слоя, разделённые по признаку *что именно делает каждый из них ложным*.

| Слой | Содержимое | Почему остаётся истинным |
|---|---|---|
| **Derived** | Карта символов, результаты тестов, показатели здоровья | Пересобирается из кода и штампуется исходным коммитом. Устаревший артефакт — это падение сборки, а не уверенный неверный ответ |
| **Decided** | Продукт, non-goals, сущности, глоссарий, решения | Неизменяемый. Решение — исторический факт: отменяется более поздней записью, которая его называет, а не переписыванием истории |
| **Ephemeral** | Сессии, лизы, заявки, лимиты | На сессию, в gitignore, с TTL, подчищается ридером |

Каждое утверждение несёт **git blob-хеши** кода, который описывает — никаких mtime,
которые checkout worktree сбрасывает целиком. Когда предмет утверждения переписан,
утверждение подавляется и учитывается, а не удаляется молча.

Каждая сущность называет канонический идентификатор и его файл. Переименование
**валит валидацию**, вместо того чтобы оставить память описывающей то, чего больше нет.

### Сессии видят друг друга

```
OTHER LIVE SESSIONS (2) — do not edit files they hold:
  - a3f81c22 on feat/export  [ledger-export]  active 40s ago
      touched: src/billing.js, src/csv.js
      holds: src/billing.js
      task: Add CSV export to src/billing.js

IN FLIGHT:
  - 0004 Fix rounding in invoice totals  [b7d29e01]
NEXT:
  - 0005 Add client search
(12 done)

health: 3 live session(s), 1 reaped, 4 open item(s), 1 stale (suppressed)
```

Правка файла, который держит другая живая сессия, **запрещается** с указанием владельца.
Лизы и заявки упавшей сессии освобождаются ридером — в том worktree, где они лежат, — а
не висят в работе вечно.

Замолчавшая сессия **не** считается мёртвой. Реапинг по молчанию означал, что основатель,
подумавший пятнадцать минут, возвращался в сессию, где все гейты перестали что-либо
принуждать; теперь смерть требует, чтобы процесс исчез или его pid был переиспользован.

### Журнал работ, который мержится

`.claude/claude-bestpractice/plan/{next,doing,done}/` — один файл на задачу, состояние закодировано
в директории, поэтому переход — это `git mv`. Пять worktree дают пять чистых add вместо
пяти конфликтующих кусков в одном JSON. Идентификаторы выделяются с оглядкой на все
соседние worktree, потому что worktree делят пространство имён ещё до того, как их файлы
вообще закоммичены.

### Ложное «готово» стоит дорого

Stop-гейт **выбрасывает прозу агента** и сам запускает ваш набор тестов. Чего он не
делает — так это не считает это доказательством, и честную формулировку стоит привести
прямо, потому что шесть раундов состязательной проверки разобрали прежнюю четыре раза:

> **Агент пишет ваш код, ваши тесты, вашу тестовую команду и файлы сборки. Любая
> проверка, читающая вывод раннера, читает то, что агент может написать.** Этот гейт
> поднимает цену ложного зелёного с одной строки до нескольких намеренных шагов и
> оставляет след каждого. Невозможным он его не делает.

Ломали так, по порядку: верил файлу-артефакту (побеждён XML, написанным руками), потом
коду возврата (`-` перед рецептом в Makefile), потом словам «N failed» (перестать их
печатать), потом счётчику «N passed» (`@echo '2 passed in 0.03s'`). Каждый фикс двигал
подделку на уровень вниз, а не наружу.

Поэтому один сигнал взят вне этого круга: гейт **сам считает объявления тестов в вашем
дереве исходников** и сравнивает. Прогон, выполнивший долю от объявленного, записывается
как неподтверждённый, а не зелёный; а красную сюиту нельзя снять деревом, потерявшим
тесты — «удалить падающий тест» это то, к чему блокирующий гейт подталкивает сильнее
всего, и это был самый дешёвый выход. Сдвинуть это число значит написать настоящие тесты,
и такую цену плагин назначает охотно.

Начиная со стадии выше прототипа он дополнительно перезапускает тесты на чистом checkout
закоммиченного дерева — это ловит класс «зелено у меня, красно у всех»: незакоммиченный
файл, локальная переменная окружения.

Он также эскалирует, а не заклинивает: после четырёх заблокированных попыток он
записывает неподтверждённое завершение и отпускает ход, потому что гейт, который вечно
блокирует работу основателя, — это гейт, который сносят.

### Слоп ловится механически — в вашем репозитории

По строкам, добавленным этим ходом, ревьюер коммитов помечает семь классов: проглоченные
исключения, голые `except`, отладочные остатки, отключённую верификацию, пропущенные
тесты, инъекции в shell и в SQL — плюс всё, что выглядит как креденшл. Уже существовавшие
совпадения игнорируются, так что вам не выставляют счёт за чужую историю.

Он **сообщает**, а не блокирует. Ревьюер, отклоняющий коммит из-за стиля, — это ревьюер,
которого выключают через неделю, а выключенный не ловит ничего.

### И строгие правила — они собственные, этого проекта

`Args:` / `Returns:` / `:param:` запрещены, потому что типы уже это сказали; изменение
сигнатуры без изменения докстринга валит сборку; абстракции с одним вызывающим,
compat-прослойки без потребителей, дублирующиеся блоки и неиспользуемые параметры —
вечный бюджет ноль; сложность и длина по храповику, только вниз.

**Это применяется к этому репозиторию, а не к вашему.** Оно лежит в `tools/` и
принуждается собственным `make check`. Оно здесь, чтобы его прочли, сверили с исходником,
который вы собираетесь установить, и скопировали, если захотите — не потому что ваш код
освобождён, а потому что плагин, навязывающий вашей сборке стиль своего автора, — это
плагин, который сносят в первый же день.

> Если вы собираетесь написать комментарий о том, чем является значение, напишите тип.

### Строгость масштабируется сама

Стадия вычисляется по репозиторию, никогда не настраивается, и храповик только затягивает.

| Обнаруженный сигнал | Что включается |
|---|---|
| CI плюс цель деплоя | Правила egress, шлюз для продовых сигналов |
| Миграция, создающая таблицу users, или auth SDK | Гейт миграций, запрет промоушена в прод, своя БД и порт на worktree |


### Проверки выполняются локально, а не на чужом счётчике

Первая же ваша сессия ставит **pre-push хук**, который прогоняет ваш собственный
`make check` или тестовую команду вашего проекта до того, как что-либо покинет машину.
Бесплатно и вовремя, чтобы остановить push, а не прислать письмо постфактум.

Если раннера этой команды на месте не окажется, хук **откажет**, а не провалится дальше
до бодрого нулевого кода: тесты у проекта есть, значит push, отчитавшийся как проверенный
без единой запущенной проверки — единственный исход хуже красного. Репозиторий, где тестов
нет вовсе, — другой случай, и он пропускается: там нечего пропускать.

Это дефолт, потому что hosted-минуты платные, а этот режим работы тратит их как целая
команда: три–восемь сессий пушат весь день, счёт один.

Поставляемый workflow GitHub Actions **закрыт переменной репозитория** и не стоит ничего,
пока вы его не включите:

```sh
claude-bp-ci status     # что где выполняется
claude-bp-ci github     # включить hosted CI (ставит переменную через gh)
claude-bp-ci off        # снять pre-push хук
```

Одно другому не мешает: pre-push хук обязывает только те машины, где он установлен,
поэтому репозиторию, куда пушат другие, hosted-прогон всё равно нужен. Разовый обход —
`git push --no-verify`: это осознанное действие, оставляющее след, в отличие от молча
пропущенного hosted-прогона.

### Он забирает под себя то, что ему мешает

`claude-bp adopt` находит другие инструменты, оспаривающие события, которыми владеет
этот плагин, помещает их хук-записи в помеченный блок с бэкапом и говорит, какие именно
конкурирующие плагины отключить. Обратимо через `--restore`. Он никогда не удаляет чужую
конфигурацию молча.

---

## Команды

| | |
|---|---|
| `claude-bp status` | Сессии, план, знание, здоровье памяти, конфликты, следующее действие |
| `claude-bp init` | Вывести слой знания из вашего кода |
| `claude-bp adopt` | Забрать события, которые оспаривает другой инструмент |
| `claude-bp doctor` | Доказать каждый гейт попыткой заведомо плохого действия |
| `claude-bp-plan` | Журнал работ: `add`, `list`, `claim`, `done` |
| `claude-bp-decide` | Принять решение, черновик которого собран из ваших же поправок |
| `claude-bp-ingest` | Санировать продовые ошибки в файлы задач с фенсингом |
| `claude-bp-knowledge` | Валидировать слой decided, обновить его индекс |
| `claude-bp-reindex` | Снести и пересобрать всё выводимое |
| `claude-bp-ci` | Где выполняются проверки: локально перед push по умолчанию, hosted CI по желанию |
| `claude-bp-attempt` | Журнал тупиков: что пробовали и почему не вышло |
| `claude-bp-options` | Записать решение как сравнение вариантов по метрикам |
| `claude-bp-ship` | Что доставила эта ветка — для того, кто не читает код (`--pr` откроет PR) |

В сессии: `/claude-bestpractice:status` · `/claude-bestpractice:plan` · `/claude-bestpractice:review`

## Гейты

| Гейт | Событие | Поведение при сбое | Что делает |
|---|---|---|---|
| `setup` | Setup | fails open | Выводит слой знания, создаёт план, засевает стадию |
| `session-start` | SessionStart | fails open | Подчищает мёртвых, регистрирует, вкидывает борд + план + стадию |
| `prompt-capture` | UserPromptSubmit | fails open | Записывает задачу дословно. **Ничего не вкидывает** |
| `pre-tool` | PreToolUse | **fails closed** | Потолок вызовов, разрыв цикла, скан секретов до записи, лизы, гейт миграций и деплоя |
| `review-commit` | `if: Bash(git commit:*)` | async rewake | Ревьюит дифф этого хода; будит только когда есть что сказать |
| `worktree-create` | WorktreeCreate | fails open | Именует, засевает доверие, выводит приватный порт и БД |
| `subagent-brief` | SubagentStart | fails open | Non-goals, сущности и карта под запрос — агентам, которые не наследуют правил |
| `checkpoint` | PreCompact | fails open | Экстрактивный чекпойнт, ноль вызовов модели, секреты вычищены |
| `evidence-gate` | Stop | **fails closed** | Дрейф скоупа, тестовое доказательство, чистый повторный прогон; собирает черновики решений |

Девять записей против самоограничения в двенадцать. Постоянный контекст **~332 токена**
против потолка в 400 — примерно 0.1 % окна на 200k.

---

## Проверено

```
make check    # lint · docs gate · slop gate · polyglot gate · knowledge · 738 тестов · 26 проверок доктора · budget
```

Доктор доказывает гейты **попыткой сделать плохое**, а не чтением конфигурации обратно —
readback конфига не способен обнаружить изменение семантики. Семнадцать реальных багов,
найденных за разработку, были невидимы для чтения кода и пойманы только исполнением,
включая дедлок, где evidence-гейт требовал тестовый артефакт, а затем блокировал сессию
за то, что она его произвела.

Набор включает полный жизненный цикл проекта, прогоняемый целиком через настоящие
исполняемые гейты: онбординг незнакомого репозитория, планирование, утёкший креденшл,
нарушение скоупа, падающий набор тестов, зелёное завершение и вторую сессию, читающую
историю.

## Обновление

```sh
claude plugin marketplace update claude-bestpractice
claude plugin update claude-bestpractice@claude-bestpractice
```

Дальше — **перезапустить Claude Code**. Обновление кладётся в новую директорию, а каждая
уже запущенная сессия продолжает исполнять старую копию, пока её не перезапустят: CLI
говорит `Restart to apply changes.` один раз и больше об этом не напоминает. Сессия,
работающая на устаревшей копии, теперь сама пишет об этом на доске — больше узнать об
этом неоткуда.

Вторая команда требует **квалифицированной** формы `name@marketplace`. `install` короткое
имя принимает, `update` — нет: короткая форма отвечает `Plugin "claude-bestpractice" not
found` при установленном и включённом плагине, и это читается как сломанная установка, а
не как неверный аргумент. Проверено обновлением 1.0.1 на 1.0.2 на живой установке.

**Строка версии — это ключ обновления, и знать это стоит, потому что иначе вас молча
замораживает.** `claude plugin update` сравнивает установленную версию с версией
маркетплейса и на этом останавливается. Измерено, а не выведено: локальный маркетплейс,
установка, изменённый файл с нетронутой версией, затем

```
$ claude plugin update claude-bestpractice@claude-bestpractice
claude-bestpractice is already at the latest version (1.0.0).
```

Изменённый файл до кэша не доехал. Между «всё актуально» и «навсегда застрял» нет никакой
наблюдаемой разницы — обе печатают галочку. Поэтому изменение под `plugin/` без поднятия
версии теперь **валит собственную сборку этого проекта**, в `tools/check_shipped.py`.
Гейт существует потому, что альтернатива — исправление, которое не может дойти до того,
кому оно нужно.

Ваше состояние обновление не трогает, и это тоже проверено, а не предположено: задача
плана, записанная под 1.0.1, осталась на месте и читалась после. Она живёт в вашем
репозитории и в общей git-директории, а не в кэше плагина, который и заменяется при
поднятии версии.

Ставили через `install.sh`? Это клон, поэтому он обновляется через `git pull` в той
директории, куда вы клонировали, и поднятие версии для этого не нужно.

## Требования

Python 3.9+ и git. **Никаких других зависимостей, по конструкции** — эти хуки бегут на
каждом вызове инструмента, поэтому дерево зависимостей — это латентность, лишний режим
отказа и supply-chain поверхность для компонента, вся работа которого — быть достойным
доверия. Проверяется в CI.

Протестировано на Python 3.9, 3.11 и 3.13. `claude plugin validate --strict` проходит
на Claude Code 2.1.220.

### Что появится в вашем репозитории и что с этим делать

Две директории, и разница между ними существенна:

**`.claude/claude-bestpractice/` — это коммитить.** Задачи, решения, тупики, маркер стадии.
Лежит внутри репозитория сознательно, потому что должно ехать вместе с веткой: решение,
принятое на `feat/billing`, — это решение про `feat/billing`, а список задач, который не
переживает переключение ветки, хуже, чем его отсутствие. Один файл на элемент, поэтому
пять воркtree дают пять чистых add вместо пяти конфликтующих кусков в одном блобе.
Ничего меняющегося от запуска к запуску там нет, так что оно не всплывает диффом каждый
раз, когда срабатывает гейт.

**`.git/claude-bestpractice/` — игнорировать, git уже игнорирует.** Живые сессии, лизы, счётчик
петель, чек теста. Лежит в общей git-директории, потому что это единственный путь,
который разделяют все воркtree одного клона, невидим для git, переживает переключение
веток и умирает вместе с клоном. Полностью восстановим: `claude-bp-reindex` пересобирает
его из закоммиченной половины, и этот путь протестирован, а не предположен.

Руками не редактируется ни то, ни другое. Если хотите убрать всё — удалите обе
директории и снесите плагин; больше на машине ничего не тронуто.

### Стоимость контекста

Панель `/plugin` в Claude Code показывает, сколько плагин добавляет в контекстное окно
на каждом ходу, — то есть это число, которое можно сравнить, а не обещание, в которое
надо поверить.

claude-bestpractice держит **~332 токена** постоянного контекста в четырёх компонентах при
самоназначенном потолке в 400. `make check` роняет сборку при превышении, и потолок
держали урезанием описаний, а не поднятием планки — дважды.

Всё остальное грузится по требованию: скиллы — только когда срабатывает их триггер,
доска — только на старте сессии, журнал работ — только когда его спросили. Бюджет
существует потому, что стоимость видна в вашей же панели расхода, а польза
контрфактуальна и невидима, — и это размен, который основатель имеет право проверить.

---

## Чем это сознательно не является

- **Не движок памяти.** Харнесс хранит и загружает память. Этот плагин владеет курацией.
- **Не код-ревьюер.** Есть несколько first-party путей ревью; выберите один и интегрируйте.
- **Не таск-менеджер.** Нативная система задач поглощается и гейтится, но не заменяется.
- **Не для команд.** Каждый компромисс предполагает одного владельца и отсутствие ревьюера.

## Четыре вещи, которые он не может принудить

Сказано прямо, потому что нормой в этой категории является ложная уверенность.

1. **Семантика тестов.** Ни один матчер не отличает оправданный skip от жульничества.
2. **Вкус.** Ни один матчер не отличает хороший дизайн от плохого.
3. **`claude --bare`.** Он сбрасывает и managed-, и плагинные хуки. Именно поэтому
   существует слой репозитория — настоящие git-хуки, CI, защита веток.
4. **Человек с root.** По замыслу.

---

## Документация

| | |
|---|---|
| [`DESIGN.md`](DESIGN.md) | Тезис, архитектура, модель памяти, субстрат, верификация |
| [`ENFORCEMENT.md`](ENFORCEMENT.md) | Binding против advisory, бюджет в десять правил, что не держится |
| [`ECONOMICS.md`](ECONOMICS.md) | Бюджет токенов, инварианты prompt-cache, admission control под rate limit |
| [`EVIDENCE.md`](EVIDENCE.md) | Каждое измеренное утверждение с источником и уровнем доказательности |
| [`LIMITS.md`](LIMITS.md) | **Чего всё ещё стоит ложный зелёный** — восемь раундов атак и те, что до сих пор работают |
| [`ROADMAP.md`](ROADMAP.md) | Что уехало, и баги, которые нашло только исполнение |

MIT.
